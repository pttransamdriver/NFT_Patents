# =============================================================================
# BACKEND ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
# This backend handles CORS proxy, metadata storage, and IPFS uploads
# =============================================================================

# API Keys
INFURA_API_KEY=your_infura_api_key_here
ETHERSCAN_API_KEY=your_etherscan_api_key_here
USPTO_API_KEY=your_uspto_api_key_here

# Network-specific private keys for deployment
SEPOLIA_PRIVATE_KEY=your_sepolia_private_key_here
PRIVATE_KEY=your_private_key_here

# SerpApi key for Google Patents search (REQUIRED for patent search)
SERPAPI_KEY=your_serpapi_key_here

# Backend Configuration
PORT=3001
VITE_API_BASE_URL=http://localhost:3001

# Contract addresses (updated after deployment)
VITE_PSP_TOKEN_ADDRESS=your_psp_token_address_here
VITE_SEARCH_PAYMENT_ADDRESS=your_search_payment_address_here
VITE_PATENT_NFT_ADDRESS=your_patent_nft_address_here
VITE_MARKETPLACE_ADDRESS=your_marketplace_address_here

# =============================================================================
# PINATA IPFS STORAGE (Backend-only - NEVER expose to frontend)
# =============================================================================
# Get your JWT token at: https://pinata.cloud
# This is stored ONLY on the backend for security
# Frontend calls /api/pinata/* endpoints instead of Pinata directly
PINATA_JWT=your_pinata_jwt_token_here

# IPFS Gateway for metadata retrieval (backend uses IPFS_GATEWAY, frontend uses VITE_IPFS_GATEWAY)
IPFS_GATEWAY=https://ipfs.io/ipfs/

# =============================================================================
# VERCEL DEPLOYMENT (Production)
# =============================================================================
# When deploying to Vercel, add these variables in the dashboard:
# - SERPAPI_KEY
# - PINATA_JWT
# - CORS_ORIGIN=https://nft-patents.vercel.app
# - NODE_ENV=production
#
# Vercel KV variables (auto-added when you create KV database):
# - KV_REST_API_URL
# - KV_REST_API_TOKEN
# - KV_REST_API_READ_ONLY_TOKEN
# =============================================================================
